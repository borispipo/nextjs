export const geometry  = {
	type:"geometry",
    dataSourceType : ["postgres"],
};
export const geography = {
	type:"geography",
    dataSourceType : ["postgres"]
};
export const st_geometry = {
	type:"st_geometry",
    dataSourceType : ["sap"]
};
export const st_point = {
	type:"st_point",
    dataSourceType : ["sap"]
};